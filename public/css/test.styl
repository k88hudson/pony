green = #3FB58E;
red = #D93B3B;

body {
  font-family: "source-code-pro";
}

p {
  line-height: 30px;
  background: red;
}

.btn {
  font-family: inherit;
  font-size: 1em;
  background: blue;
  border: none;
  display: inline-block;
  padding: 0;
  margin: 0;
  line-height: normal;
}

.icon {
  background: green;
}
